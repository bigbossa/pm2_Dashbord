# ระบบจัดการสิทธิ์การเข้าถึงโปรแกรม

## 📋 ภาพรวมระบบ

ระบบนี้จัดการสิทธิ์การเข้าถึงโปรแกรมต่างๆ โดยแบ่งผู้ใช้เป็น 2 ประเภท:
1. **IT Department** - มีสิทธิ์เข้าถึงและตั้งค่าทุกอย่าง
2. **User ทั่วไป** - เข้าถึงได้เฉพาะโปรแกรมที่ได้รับสิทธิ์

## 🔐 การทำงานของระบบ

### 1. Login (http://192.168.19.37:3000/)
- ผู้ใช้ login ด้วย username และ password
- ระบบตรวจสอบข้อมูลจากฐานข้อมูล
- หลัง login สำเร็จจะ redirect ไปหน้า `/programs.html`

### 2. หน้าเลือกโปรแกรม (http://192.168.19.37:3000/programs.html)
**สำหรับ IT:**
- แสดงโปรแกรมทั้งหมดในระบบ
- มีป้าย "👑 IT Admin" บนการ์ดโปรแกรม
- สามารถเข้าถึงทุกโปรแกรมได้

**สำหรับ User ทั่วไป:**
- แสดงเฉพาะโปรแกรมที่ได้รับสิทธิ์จาก Admin
- แสดง Role ที่ได้รับ (เช่น Admin, User, Viewer)
- ไม่สามารถเข้าหน้าตั้งค่าได้

### 3. หน้าตั้งค่า (เฉพาะ IT เท่านั้น)
- **Setup Page** - ตั้งค่าโปรแกรมและ Role
- **All Users** - จัดการผู้ใช้ทั้งหมด
- **Add User** - เพิ่มผู้ใช้ใหม่
- **Edit User** - แก้ไขข้อมูลและสิทธิ์ผู้ใช้

## 🛠️ ไฟล์ที่เพิ่ม/แก้ไข

### Backend (Dashboard)

#### 1. `dashboard/middleware/auth.js`
```javascript
// เพิ่ม middleware 3 ตัว:
- requireAuth()           // ตรวจสอบว่า login หรือยัง
- requireIT()             // ตรวจสอบว่าเป็น IT หรือไม่
- requireProgramAccess()  // ตรวจสอบสิทธิ์เข้าถึงโปรแกรมเฉพาะ
```

#### 2. `dashboard/routes/userRoutes.js`
```javascript
// เพิ่ม API:
- GET /api/my-programs    // ดึงรายการโปรแกรมที่มีสิทธิ์
```

### Frontend (Dashboard)

#### 3. `dashboard/public/programs.html`
หน้าแสดงรายการโปรแกรมทั้งหมดที่ user สามารถเข้าถึงได้

#### 4. `dashboard/public/programs.js`
- โหลดรายการโปรแกรมจาก API
- แสดงป้าย IT Admin สำหรับผู้ใช้ IT
- จัดการ logout

#### 5. `dashboard/public/login.js`
- แก้ไขให้ redirect ไป `/programs.html` แทน `/index.html`

#### 6. `dashboard/public/auth-check.js`
- เพิ่มพารามิเตอร์ `requireIT` เพื่อบังคับให้เป็น IT
- ตรวจสอบสิทธิ์ก่อนเข้าหน้า

#### 7. หน้าจัดการ User (ป้องกันด้วย IT)
- `setup.js` - เพิ่ม `checkAuthAndDisplayUser(true)`
- `all_users.js` - เพิ่ม `checkAuthAndDisplayUser(true)`
- `edit_user.js` - เพิ่ม `checkAuthAndDisplayUser(true)`

## 📊 Database Tables ที่ใช้

```sql
-- ตาราง programs
program_id, program_name, program_code

-- ตาราง roles
role_id, role_name

-- ตาราง useryc
iduser, username, password, usersname, department, site, is_active

-- ตาราง user_program_auth (สิทธิ์การเข้าถึง)
iduser, program_id, role_id
```

## 🔑 Program Codes

```javascript
HOMECARE  → /homecare
REPAIR    → /repair
AUTOPO    → /autopo
YCSALES   → /ycsalescrm
```

## 🚀 วิธีใช้งาน

### สำหรับ IT Admin:
1. Login ด้วย account ที่ department = "IT"
2. จะเห็นโปรแกรมทั้งหมดพร้อมป้าย "IT Admin"
3. สามารถเข้า Setup, All Users, Add User, Edit User ได้
4. คลิกโปรแกรมที่ต้องการใช้งาน

### สำหรับ User ทั่วไป:
1. Login ด้วย account ของตัวเอง
2. จะเห็นเฉพาะโปรแกรมที่ Admin ให้สิทธิ์
3. ถ้าพยายามเข้าหน้า Setup จะถูก block และ redirect กลับ
4. คลิกโปรแกรมที่ต้องการใช้งาน

## 🔒 Security Features

1. **Session-based Authentication** - ใช้ express-session
2. **Department-based Access Control** - IT มีสิทธิ์พิเศษ
3. **Program-level Authorization** - ตรวจสอบสิทธิ์ต่อโปรแกรม
4. **Frontend & Backend Validation** - ป้องกันทั้ง 2 ฝั่ง
5. **Automatic Redirect** - redirect ไปหน้าที่เหมาะสมอัตโนมัติ

## 📝 ตัวอย่างการใช้งาน Middleware

```javascript
// ในหน้าที่ต้องการ IT เท่านั้น
await checkAuthAndDisplayUser(true); // requireIT = true

// ในหน้าที่ login แล้วก็เข้าได้
await checkAuthAndDisplayUser(); // ไม่บังคับ IT

// ใน API Route
router.get('/api/my-programs', requireAuth, async (req, res) => {
    // ต้อง login ก่อน
});

router.post('/api/programs', requireIT, async (req, res) => {
    // ต้องเป็น IT เท่านั้น
});
```

## 🎯 Flow การทำงาน

```
1. User เปิด http://192.168.19.37:3000/
   ↓
2. Login Page
   ↓
3. ตรวจสอบ Username/Password
   ↓
4. บันทึก Session
   ↓
5. Redirect to /programs.html
   ↓
6. ตรวจสอบ Department
   ├─ IT → แสดงทุกโปรแกรม + สามารถเข้า Setup ได้
   └─ User → แสดงเฉพาะโปรแกรมที่มีสิทธิ์
   ↓
7. เลือกโปรแกรมที่ต้องการ
   ↓
8. เข้าใช้งานโปรแกรม
```

## 🐛 Troubleshooting

### ปัญหา: Login แล้วไม่เห็นโปรแกรม
- ตรวจสอบว่ามีการกำหนดสิทธิ์ในตาราง `user_program_auth` หรือไม่
- ตรวจสอบว่า program_code ตรงกับใน `programs.js` หรือไม่

### ปัญหา: IT login แล้วไม่เห็นป้าย Admin
- ตรวจสอบว่า `department` ใน database เป็น "IT" (case-sensitive)

### ปัญหา: User พยายามเข้าหน้า Setup ได้
- ตรวจสอบว่ามี `checkAuthAndDisplayUser(true)` ในไฟล์ .js หรือไม่
- ตรวจสอบว่า auth-check.js โหลดก่อนไฟล์หลักหรือไม่

## ✅ Testing Checklist

- [ ] IT สามารถเข้า /programs.html และเห็นโปรแกรมทั้งหมด
- [ ] IT สามารถเข้า /setup.html ได้
- [ ] IT สามารถเข้า /all_users.html ได้
- [ ] User เห็นเฉพาะโปรแกรมที่มีสิทธิ์
- [ ] User ไม่สามารถเข้า /setup.html (redirect กลับ)
- [ ] User ไม่สามารถเข้า /all_users.html (redirect กลับ)
- [ ] API /api/my-programs return ข้อมูลถูกต้อง
- [ ] Logout ทำงานปกติ

## 🔄 Next Steps (แนะนำ)

1. เพิ่มการ log activity ของ user
2. เพิ่มระบบ forgot password
3. เพิ่มการแจ้งเตือนเมื่อมีการเปลี่ยนแปลงสิทธิ์
4. เพิ่ม Role-based permissions ภายในโปรแกรม
5. เพิ่ม 2FA สำหรับ IT account
