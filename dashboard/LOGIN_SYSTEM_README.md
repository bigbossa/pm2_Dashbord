# Login System - คู่มือการใช้งาน

## ภาพรวมระบบ Login

ระบบ Login Page ถูกเพิ่มเข้ามาใน UserYC Application เพื่อรักษาความปลอดภัยและจัดการ Session ของผู้ใช้งาน

## ไฟล์ที่เพิ่มเข้ามา

### Frontend Files (public/)
- **login.html** - หน้า Login UI พร้อม form สำหรับกรอก username และ password
- **login.js** - JavaScript สำหรับจัดการการ Login, validation และ redirect
- **auth-check.js** - JavaScript ที่ใช้ตรวจสอบ session ในทุกหน้า และมีฟังก์ชัน logout

### Backend Files
- **middleware/auth.js** - Middleware สำหรับป้องกันการเข้าถึง API routes ที่ต้องการ authentication

### อัปเดตไฟล์เดิม
- **server.js** - เพิ่ม express-session middleware
- **routes/userRoutes.js** - เพิ่ม API endpoints: /api/login, /api/logout, /api/check-session
- **package.json** - เพิ่ม express-session dependency
- **.env** - เพิ่ม SESSION_SECRET สำหรับเข้ารหัส session
- **public/index.html** - เพิ่มปุ่ม Logout และแสดงข้อมูล user
- **public/all_users.js** - เชื่อมต่อกับระบบ authentication

## วิธีใช้งาน

### 1. การติดตั้ง Dependencies
```bash
npm install express-session
```

### 2. การตั้งค่า Environment Variables
ในไฟล์ `.env` ตรวจสอบว่ามี SESSION_SECRET:
```
SESSION_SECRET=your-super-secret-key-change-in-production-2026
```

### 3. การเริ่มต้นใช้งาน

1. **สร้าง User ในฐานข้อมูล**
   - ต้องมี user อย่างน้อย 1 user ในตาราง `useryc`
   - Password จะถูก hash ด้วย bcrypt อัตโนมัติตอนสร้าง user

2. **เข้าสู่ระบบ**
   - เปิดเบราว์เซอร์ไปที่ `http://localhost:3010/login.html`
   - กรอก Username และ Password
   - คลิก "เข้าสู่ระบบ"

3. **การใช้งาน**
   - หลังจาก Login สำเร็จจะถูก redirect ไปหน้า index.html
   - ทุกหน้าจะแสดงชื่อผู้ใช้ที่ Login อยู่มุมบนซ้าย
   - มีปุ่ม "ออกจากระบบ" มุมบนขวา

4. **ออกจากระบบ**
   - คลิกปุ่ม "🚪 ออกจากระบบ"
   - ระบบจะลบ session และ redirect กลับหน้า login

## API Endpoints

### POST /api/login
เข้าสู่ระบบ
```json
Request:
{
  "username": "admin",
  "password": "password123"
}

Response (Success):
{
  "status": "success",
  "message": "เข้าสู่ระบบสำเร็จ",
  "user": {
    "iduser": 1,
    "username": "admin",
    "usersname": "ผู้ดูแลระบบ",
    "department": "IT",
    "site": "BKK"
  }
}

Response (Error):
{
  "status": "error",
  "message": "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง"
}
```

### POST /api/logout
ออกจากระบบ
```json
Response:
{
  "status": "success",
  "message": "ออกจากระบบสำเร็จ"
}
```

### GET /api/check-session
ตรวจสอบสถานะการ login
```json
Response (Logged In):
{
  "loggedIn": true,
  "user": {
    "iduser": 1,
    "username": "admin",
    "usersname": "ผู้ดูแลระบบ",
    "department": "IT",
    "site": "BKK"
  }
}

Response (Not Logged In):
{
  "loggedIn": false
}
```

## Session Management

### การตั้งค่า Session
```javascript
app.use(session({
    secret: process.env.SESSION_SECRET,
    resave: false,
    saveUninitialized: false,
    cookie: {
        secure: false,      // ตั้งเป็น true ถ้าใช้ HTTPS
        httpOnly: true,     // ป้องกัน XSS attacks
        maxAge: 24 * 60 * 60 * 1000  // 24 ชั่วโมง
    }
}));
```

### การจัดเก็บข้อมูล Session
```javascript
req.session.user = {
    iduser: user.iduser,
    username: user.username,
    usersname: user.usersname,
    department: user.department,
    site: user.site
};
```

## Security Features

1. **Password Hashing** - รหัสผ่านถูก hash ด้วย bcrypt (10 rounds)
2. **Session Management** - ใช้ express-session จัดการ session
3. **httpOnly Cookies** - ป้องกัน JavaScript ฝั่ง client เข้าถึง cookie
4. **Auto Redirect** - หน้าที่ต้อง login จะ redirect อัตโนมัติถ้ายังไม่ได้ login
5. **Account Status Check** - ตรวจสอบว่า user ยัง active อยู่หรือไม่

## การป้องกัน Routes (Optional - ยังไม่ได้ใช้งาน)

หากต้องการป้องกัน API routes ให้ใช้ middleware `requireAuth`:

```javascript
const { requireAuth } = require('./middleware/auth');

// ปกป้อง route
router.get('/users', requireAuth, async (req, res) => {
    // เฉพาะ user ที่ login แล้วถึงจะเข้าถึงได้
});
```

## การทดสอบ

### Test Case 1: Login สำเร็จ
1. เปิด `http://localhost:3010/login.html`
2. กรอก username และ password ที่ถูกต้อง
3. คลิก "เข้าสู่ระบบ"
4. ✅ ต้องแสดง "เข้าสู่ระบบสำเร็จ" และ redirect ไป index.html

### Test Case 2: Login ไม่สำเร็จ
1. กรอก username หรือ password ผิด
2. คลิก "เข้าสู่ระบบ"
3. ✅ ต้องแสดง error message สีแดง

### Test Case 3: เข้าหน้าโดยไม่ Login
1. เปิดเบราว์เซอร์ใหม่ (Clear cookies)
2. พยายามเข้า `http://localhost:3010/index.html`
3. ✅ ต้อง redirect ไป login.html อัตโนมัติ

### Test Case 4: Logout
1. Login เข้าระบบ
2. คลิกปุ่ม "ออกจากระบบ"
3. ✅ ต้อง redirect ไป login.html
4. พยายามกลับไป index.html
5. ✅ ต้อง redirect ไป login.html อีกครั้ง

### Test Case 5: Session Persistence
1. Login เข้าระบบ
2. รีเฟรชหน้า (F5)
3. ✅ ยังคง login อยู่ ไม่ต้อง login ใหม่

## Troubleshooting

### ปัญหา: ไม่สามารถ Login ได้
- ตรวจสอบว่า user มีอยู่ในฐานข้อมูล
- ตรวจสอบ `is_active = true`
- ดูใน console เบราว์เซอร์มี error อะไร

### ปัญหา: Login แล้ว redirect กลับ login.html
- ตรวจสอบว่า SESSION_SECRET ตั้งค่าใน .env แล้ว
- ตรวจสอบว่า express-session ถูกติดตั้งแล้ว
- ลองลบ cookies และ login ใหม่

### ปัญหา: Server Error 500
- ดู log ใน terminal
- ตรวจสอบการเชื่อมต่อฐานข้อมูล
- ตรวจสอบว่า bcrypt ติดตั้งถูกต้อง

## Structure Overview

```
UserYC/
├── middleware/
│   └── auth.js              # Authentication middleware
├── public/
│   ├── login.html           # Login page UI
│   ├── login.js             # Login functionality
│   ├── auth-check.js        # Session check for all pages
│   ├── index.html           # Main page (with logout button)
│   ├── all_users.js         # Updated with auth
│   └── ... (other pages)
├── routes/
│   └── userRoutes.js        # Updated with login/logout routes
├── .env                      # Environment variables
├── server.js                 # Updated with session middleware
└── package.json              # Updated with express-session
```

## สรุป

ระบบ Login Page ช่วยให้:
- ✅ มีการจัดการ Session อย่างปลอดภัย
- ✅ ป้องกันการเข้าถึงหน้าต่างๆ โดยไม่ได้ login
- ✅ แสดงข้อมูล user ที่ login อยู่
- ✅ มีระบบ Logout ที่ทำงานได้ถูกต้อง
- ✅ Password ถูก hash อย่างปลอดภัย

---

**หมายเหตุ:** สำหรับ Production ควรเปลี่ยน SESSION_SECRET ให้เป็นค่าที่ซับซ้อนและปลอดภัยมากขึ้น และตั้ง `cookie.secure = true` เมื่อใช้ HTTPS
